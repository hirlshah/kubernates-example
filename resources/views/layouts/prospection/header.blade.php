{{-- Header --}}
<nav class="navbar navbar-expand-lg bg-3c">
    <div class="container-fluid">
        <a class="navbar-brand text-center mx-auto" href="{{ route('seller-dashboard') }}">
            <img width="115" src="{{ asset(config('app.rankup.company_logo_path')) }}" alt="">
        </a>
    </div>
</nav>
{{-- Header --}}
